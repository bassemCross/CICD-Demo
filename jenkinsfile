pipeline { 
    agent any  
    stages { 
        stage('Build') { 
            steps { 
                script {
                    echo 'Maven clean install'
                    if (isUnix()) {
                        sh 'mvn clean install'
                    }else{
                        bat 'mvn clean install'
                    }
                }
            }
        }
    }
}