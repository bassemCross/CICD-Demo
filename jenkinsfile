pipeline { 
    agent any  
    stages { 
        stage('Build') { 
            steps { 
                script {
                    echo 'Build.'
                    if (isUnix()) {
                        sh 'mvn clean install'
                    }else{
                        bat 'mvn clean install'
                    }
                }
            }
        }
    }
}