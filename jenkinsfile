pipeline { 
    agent any  
    stages { 
        stage('Build') { 
            steps { 
                script {
                    echo 'Maven clean install stage'
                    if (isUnix()) {
                        sh 'mvn clean install'
                    }else{
                        bat 'mvn clean install'
                    }
                }
            }
        }
    }
}